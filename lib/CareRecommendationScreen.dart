import 'package:flutter/material.dart';

class CareRecommendationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Care Recommendation')),
      body: Center(child: Text('Care Recommendation Screen', style: TextStyle(fontSize: 24))),
    );
  }
}
