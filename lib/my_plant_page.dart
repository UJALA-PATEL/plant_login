import 'package:flutter/material.dart';

class MyPlantPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("My Plant")),
      body: Center(child: Text("My Plant Page")),
    );
  }
}
